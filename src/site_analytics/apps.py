"""Application configuration for the site_analytics project."""

from django.apps import AppConfig


class SiteAnalyticsAppConfig(AppConfig):

    name = 'site_analytics'
    verbose_name = "Site Analytics"
